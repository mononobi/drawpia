# -*- coding: utf-8 -*-

# a separator which is used between each part of each entry.
SEP = '[***]'

# group size for the draw. if the entries count is not dividable by
# the group size, an error would be raised.
# if the group size is bigger than the entries count, an
# error would also be raised.
GROUP_SIZE = 2
